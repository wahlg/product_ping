{
  "name": "product_ping",
  "private": true,
  "dependencies": {
    "@pm2/pm2-version-check": "^1.0.3",
    "pm2": "^4.5.4"
  },
  "scripts": {
    "start": "pm2-runtime start ecosystem.config.js --env production"
  }
}
